# Reading file content whose name is a special character
- We can use ./ in the path    
  Here '.' represents 'current directory' and '/\<filename>' tells it to access the contents of \<filename> within '.' directory    
  **Command:**    
  ```cat ./-```
